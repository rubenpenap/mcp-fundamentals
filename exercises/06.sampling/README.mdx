# Sampling

<div>
<!-- prettier-ignore -->
{/*
evalstate — 2:41 PM
it's the same as max_tokens when you make a completions api request (e.g. return at most 1024 tokens in the response)
system prompt + messages = input tokens. max_tokens is separate, but (system prompt + messages + OUTPUT MESSAGE) can't exceed context window size.
*/}
</div>
